# Blog Links

- Parcel: https://parceljs.org/




# Notes 

// React Element
// React.createElement(type, props, children) ==> Object ==> When we render this onto DOM, then it becomes HTML element

// const heading = React.createElement('h1', {id: 'heading'}, 'Hey Minchi! Lets do it!')
// const root = ReactDOM.createRoot(document.getElementById('root'))
// root.render(heading)

//JSX - Not HMTL inside JS file, but it looks like HTML. It is a syntactic sugar for React.createElement() method. It is not supported by browsers, so we need to use a transpiler like Babel to convert JSX into React.createElement() method calls.
// JSX - HTML like Syntax
// Babel is converting your jSX code into React.createElement() method calls. It is a transpiler that converts modern JavaScript code into a backward compatible version of JavaScript that can run in older browsers. It is also used to convert JSX into React.createElement() method calls.
// JSX ==> Babe => React.createElement() method calls ==> React Element (Object) ==> When we render this onto DOM, then it becomes HTML element

// React Component
// Class based Components - OLD
// Functional Components - NEW

// React Functional Component
// A function which is returning a piece of JSX, is a functional component.
// Component Composition - When we use a component inside another component, it is called component composition. It is a way to reuse components. It is also a way to create a hierarchy of components. It is also a way to create a parent-child relationship between components.


# Config Driven UI

